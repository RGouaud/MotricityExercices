<resources>
    <!-- Base application theme. -->

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar"/>

    <style name="AppThemeMenuPrincipal" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowNoTitle">true</item> <!-- Titre: si false, il y aura marqué LaZer en haut -->
        <item name="android:windowFullscreen">true</item> <!-- true: enlever la barre du haut avec heure, batterie, notifs... -->
        <item name="android:windowActionBar">false</item> <!-- si true laisse une barre en haut avec marqué le titre si NoTitle=false -->

        <!-- transitions -->
        <item name="android:windowContentTransitions">true</item>


    </style>


    <style name="AppThemeStaticExerciceParameter" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowNoTitle">true</item> <!-- Titre: si false, il y aura marqué LaZer en haut -->
        <item name="android:windowFullscreen">true</item> <!-- true: enlever la barre du haut avec heure, batterie, notifs... -->
        <item name="android:windowActionBar">false</item> <!-- si true laisse une barre en haut avec marqué le titre si NoTitle=false -->
        <!-- transitions -->
        <item name="android:windowContentTransitions">true</item>


    </style>

    <style name="AppThemeRythmExerciceParameter" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item> <!-- Titre: si false, il y aura marqué LaZer en haut -->
        <item name="android:windowFullscreen">true</item> <!-- true: enlever la barre du haut avec heure, batterie, notifs... -->
        <item name="android:windowActionBar">false</item> <!-- si true laisse une barre en haut avec marqué le titre si NoTitle=false -->
        <!-- transitions -->
        <item name="android:windowContentTransitions">true</item>
    </style>

    <style name="AppThemeCamera" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item> <!-- Titre: si false, il y aura marqué LaZer en haut -->
        <item name="android:windowFullscreen">true</item> <!-- true: enlever la barre du haut avec heure, batterie, notifs... -->
        <item name="android:windowActionBar">false</item> <!-- si true laisse une barre en haut avec marqué le titre si NoTitle=false -->
    </style>

    <style name="AppThemeDataManager" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item> <!-- Titre: si false, il y aura marqué LaZer en haut -->
        <item name="android:windowFullscreen">true</item> <!-- true: enlever la barre du haut avec heure, batterie, notifs... -->
        <item name="android:windowActionBar">false</item> <!-- si true laisse une barre en haut avec marqué le titre si NoTitle=false -->
    </style>

    <style name="BoutonMenuPrincipal" parent="Theme.AppCompat">
        <item name="colorButtonNormal">@color/ROUGE_CHARTE_GRAPHIQUE</item>
        <item name="colorControlHighlight">@color/GRIS_CHARTE_GRAPHIQUE</item>
    </style>

    <style name="BoutonDialogues" parent="Theme.AppCompat">
        <item name="colorButtonNormal">@color/BLANC</item>
        <item name="colorControlHighlight">@color/GRIS_CHARTE_GRAPHIQUE</item>
    </style>


</resources>